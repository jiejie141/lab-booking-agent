"""lab-booking-agent —— 智能实验室预约 Agent。

一个带真实业务约束（资质 / 容量 / 开放时间 / 已有预约 / 并发冲突）的
实验室预约系统，核心调度交给 LangGraph 编排的协商式 Agent。

设计上刻意区别于常见的「表单 + 大模型抽槽位」写法：
  * 槽位填不齐不是只追问，而是先去查约束，再把冲突权衡成备选方案；
  * 预约写入的并发安全由数据库唯一索引兜底，不依赖应用层 check-then-act；
  * Agent 不可用时整体退化为引导式表单，AI 是增强而非唯一路径。
"""

__version__ = "1.3.0"
